name: package

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Package version (e.g., 1.0.0)'
        required: true
        type: string
      publish:
        description: 'Push to NuGet.org'
        required: true
        type: boolean
        default: false
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  build-native:
    uses: ./.github/workflows/build-native.yml

  pack:
    name: dotnet pack
    needs: build-native
    runs-on: ubuntu-22.04
    permissions:
      contents: read

    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Download all native artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: soloud-native-*
          path: artifacts/native/
          merge-multiple: false

      - name: Reshape artifact layout
        run: |
          # actions/download-artifact extracts each into a folder named after the artifact;
          # rename to the RID-only folder structure the csproj expects.
          set -euo pipefail
          for src in artifacts/native/soloud-native-*; do
              rid="${src#artifacts/native/soloud-native-}"
              mv "$src" "artifacts/native/$rid"
          done
          find artifacts -type f

      - name: Determine package version
        id: version
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            echo "value=${{ inputs.version }}" >> "$GITHUB_OUTPUT"
          elif [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            echo "value=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"
          else
            echo "value=0.0.0-ci.${{ github.run_number }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and pack
        run: |
          dotnet build SoLoudSharp.sln -c Release -p:Version=${{ steps.version.outputs.value }} -p:SkipNativeWarning=true
          dotnet pack src/SoLoudSharp/SoLoudSharp.csproj -c Release -p:Version=${{ steps.version.outputs.value }} -o packages

      - name: Upload nupkgs
        uses: actions/upload-artifact@v4
        with:
          name: nupkgs
          path: packages/

      - name: Publish to NuGet.org
        if: ${{ (inputs.publish == true) || startsWith(github.ref, 'refs/tags/v') }}
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: |
          if [ -z "$NUGET_API_KEY" ]; then
            echo "::error::NUGET_API_KEY secret missing"
            exit 1
          fi
          dotnet nuget push 'packages/*.nupkg' --source https://api.nuget.org/v3/index.json --api-key "$NUGET_API_KEY" --skip-duplicate
